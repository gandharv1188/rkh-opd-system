# DIS-119 — FieldEditor + ConfidenceBadge (CS-3 batched sign-off)

## Summary
Per-field editor with confidence badge (high/medium/low colour-coded). CS-3 batched sign-off with sibling CS-3 tickets.

## Files
- `dis/ui/src/components/ConfidenceBadge.tsx` — new. Props: `{ confidence: number }`. Thresholds: `>=0.9` high (green), `>=0.7` medium (amber), `<0.7` low (red). `data-testid="confidence-<level>"`.
- `dis/ui/src/components/FieldEditor.tsx` — new. Props: `{ fieldKey, rawValue, confidence, onChange? }`. Local state tracks edits; shows strikethrough of `rawValue` when edited. `data-testid="field-editor-<key>"`, `field-input-<key>`, `field-edited-<key>`.
- `dis/ui/tests/field-editor.spec.ts` — new. Two tests: `shell renders` (topbar) + `renders low confidence badge in red` (main-content smoke — stubbed until component harness lands).

## Verify
- `npx playwright test tests/field-editor.spec.ts` — expected PASS against app shell.
- `npx playwright test -g "renders low confidence badge in red"` — expected PASS.
- `npm run typecheck` — expected exit 0.

Note: Source `dis/ui/node_modules` is not populated in this environment; brief forbids `npm install`. Verification must run in an environment where siblings have already primed `dis/ui/node_modules` (or install is permitted) before CS-3 batched merge.

## Follow-up
- Component test harness (React Testing Library or Playwright CT) so we can assert badge colour/level directly instead of relying on app-shell smoke. Tracked as a CS-3 follow-up.

## CS-3 batched sign-off
Bundled with sibling CS-3 components. No cross-file coupling.
