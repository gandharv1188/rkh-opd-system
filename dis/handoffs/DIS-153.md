# DIS-153 Handoff — OCR Outage Runbook

## 1. Ticket
DIS-153 — Author runbook for OCR provider outage (Datalab / Claude Vision).

## 2. Owner
`dev-f-rb-ocr` (dis-squad). Worktree: `.claude/worktrees/dev-f-rb-ocr`. Branch: `feat/dev-f-rb-ocr`.

## 3. Scope
Doc-only. One new runbook file + this handoff. No code, no schema, no deploy.

## 4. Files changed
- `dis/document_ingestion_service/09_runbooks/ocr-outage.md` (new, 6-step runbook)
- `dis/handoffs/DIS-153.md` (new, this file)

## 5. Approach
Followed the 6-step structure from the dispatch brief: Detect → Triage → Kill-switch → Fall-back → Recovery → Post-mortem. Added a header block (incident class, on-call placeholder, estimated impact) per runbook template. Explicit commands for Supabase Edge Function log tailing, secret flipping, and backlog reprocessing so an on-call engineer can execute without extra lookups.

## 6. Tests / Verify
- `ls dis/document_ingestion_service/09_runbooks/ocr-outage.md` → exists.
- `grep -c "^## Step" dis/document_ingestion_service/09_runbooks/ocr-outage.md` → 6 (≥ 5 required).

## 7. Risks / Open questions
- PagerDuty rotation name `DIS-ONCALL` is a placeholder — real rotation ID TBD.
- `scripts/dis/reprocess_failed_ocr.js` referenced in Step 5 does not yet exist; it is the expected drain tool. File as a follow-up ticket if not already tracked.
- Feature flag storage location (`app_config` row vs Supabase secret) should be confirmed against `05_config/feature_flags.md` once that doc lands.

## 8. Follow-ups
- Link this runbook from the DIS README index.
- Add a dry-run drill to the team's quarterly ops calendar.

## 9. Rollback
Pure doc addition — revert the commit if the runbook is rejected in review.

## 10. Reviewer checklist
- [ ] 6 steps present, each starts with `## Step N — <title>`.
- [ ] Commands are copy-pasteable and reference the correct project-ref.
- [ ] Escalation path + owner placeholder are clear.

## 11. Status
Ready for review. Task DIS-153 marked completed.
