# DIS-035 — Orchestrator retry path integration test — handoff

**Branch:** `feat/dev-b2-state-orch`
**Status:** Green
**Tags:** core, test

## Summary

Integration test at `dis/tests/integration/orchestrator-retry.test.ts`
drives the full retry path:

```
ingest (uploaded v1)
  → process (ocr_scan fails) → failed v2   [parent preserved]
  → retry                    → NEW row v1 parent_extraction_id=parent.id
```

Uses DIS-012 script-driven fakes — OCR scripted to throw, file-router
scripted to `ocr_scan`, structuring scripted but not reached.

## Test cases

1. **Retry preserves parent & links child:** parent row ends at version=2,
   status='failed'; retried row at version=1, status='uploaded',
   `parentExtractionId === parent.id`; exactly two rows in DB; parent's
   status/version unchanged after retry.
2. **Idempotency-key preservation:** parent retains the original
   idempotency key; child uses a distinct `retry:`-prefixed key so future
   ingest() lookups on the original key still resolve to the parent only.

## VERIFY

- VERIFY-1: `test -f dis/tests/integration/orchestrator-retry.test.ts` — EXISTS
- VERIFY-2: `npx vitest run tests/integration/orchestrator-retry.test.ts` — exit 0 (2/2 pass)
- VERIFY-3: `grep -cE "retry|preserve" …orchestrator-retry.test.ts` — **≥ 2** (many)

## Gate 2 invariants

- Full suite: 257/257 tests pass (250 baseline + 7 new across DIS-034/040/035).
- `npx tsc --noEmit`: exit 0.
- `node dis/scripts/fitness.mjs`: 0 violations.

## Commits

- `4ada38d` — test(DIS-035): red sentinel on retried.version
- `4f9795e` — feat(DIS-035): tighten to version=1 (green)
- this handoff

## Files touched

- **Added:** `dis/tests/integration/orchestrator-retry.test.ts`
