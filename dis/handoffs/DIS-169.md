# DIS-169 — Security Review Checklist

**Owner:** dev-f-security
**Branch:** feat/dev-f-security
**Status:** Complete

## Deliverable

`dis/document_ingestion_service/10_handoff/security-review.md` — a pre-launch security checklist for DIS, organised into 8 sections (RLS + Authz, Secrets, TLS, Input validation, XSS, Logging + audit, Dependencies, Sign-off).

**28 checkboxes total** (spec required >= 15). Each item is a concrete, verifiable check referencing the relevant DIS ticket where applicable (DIS-008, DIS-055, DIS-090, DIS-097, DIS-136, DIS-161).

## How to use

1. Copy the checklist into the release cut PR description.
2. Each reviewer tick means the check has been executed and evidence attached (log snippet, screenshot, or linked test run).
3. Sign-off block at the bottom records reviewer, date, and release tag.

## Verification

- `ls dis/document_ingestion_service/10_handoff/security-review.md` → EXISTS
- `grep -c "^- \[ \]" ...` → 28 (>= 15)

## Follow-ups (out of scope)

- Automate a subset (secrets scan, npm audit, CSP header assert) in CI.
- Wire HSTS + CSP headers into the Edge Function response path.
