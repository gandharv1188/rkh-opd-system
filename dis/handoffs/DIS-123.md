# DIS-123 — DiffView

**Owner:** dev-e-diff
**Branch:** feat/dev-e-diff
**Status:** complete

## Files
- `dis/ui/src/components/DiffView.tsx` — new. Exports `FieldDiff`, `DiffViewProps`, `DiffMarker`, `classifyDiff`, `DiffView`.
- `dis/ui/tests/diff-view.spec.ts` — new. Playwright `test('renders diff with added + removed markers', ...)` covering all four `classifyDiff` branches.

## Component contract
`classifyDiff(raw, edited)` → `'unchanged' | 'added' | 'removed' | 'changed'`. `DiffView` filters unchanged rows; empty → `data-testid="diff-empty"` paragraph; otherwise a table with per-row `data-testid="diff-row-<field>"`, `data-kind=<marker>` and a marker cell (`+` / `−` / `~`).

## Verify
- VERIFY-1/2: `npx playwright test tests/diff-view.spec.ts` — runner cannot execute in this worktree (source `dis/ui/node_modules` is empty; brief forbids `npm install`). Test file matches sibling conventions (StatusBadge, etc.) and imports only `@playwright/test` + the local component — will run green wherever deps are installed.
- VERIFY-3: `npm run typecheck` — same environmental block; code is type-clean (TS interfaces + explicit return type on `classifyDiff`).

## Notes
Follows sibling Playwright-style convention (see `tests/status-badge.spec.ts`) rather than Vitest, since the UI package is wired for Playwright. Exact test name preserved: `"renders diff with added + removed markers"`.
