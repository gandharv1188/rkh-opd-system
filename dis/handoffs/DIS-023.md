# DIS-023 — Promotion Service Handoff

Branch: `feat/dis-023-promotion`
Worktree: `.claude/worktrees/dis-023-promotion`
Owner: dev-promotion

## Scope delivered

Pure, intent-only promotion-plan builder. **No SQL executed** here —
that is the DatabasePort adapter's job. Implements TDD §13 and
clinical-safety CS-10 / CS-11.

- `dis/src/core/promotion.ts` — exports `buildPromotionPlan(verifiedExtraction, existingRowHints): PromotionPlan`.
- `dis/tests/unit/promotion.test.ts` — 8 test cases across 5 describe blocks.

## Clinical-safety coverage

- **CS-10** — `applyDischargeSummaryLatestOnly` collapses
  `discharge_summary` labs to the latest `test_date` per
  `test_name_normalized`. Superseded rows are reported in `plan.skipped`
  with `reason = 'discharge_summary_superseded'`. Non-discharge doc
  types pass through untouched.
- **CS-11** — A pre-fetched `ExistingRowHints` (labKeys / vaxKeys) is
  consulted before emitting each insert. Duplicates are captured in
  `plan.skipped` with `reason = 'duplicate_pkid_match'`. Keys match the
  DB unique indexes in `data_model.md` (`uniq_lab_dedupe`, `uniq_vax_dedupe`).

## Contract

```ts
type PromotionPlan = {
  readonly labInserts: readonly LabInsert[];
  readonly vaxInserts: readonly VaxInsert[];
  readonly documentPatch: DocumentPatch | null;
  readonly skipped: readonly { entity: 'lab' | 'vax'; reason: string; input: unknown }[];
};
```

`ExistingRowHints` is a `ReadonlySet<string>` lookup. The caller (the
orchestrator or the adapter) must pre-fetch these from the DB so the
core stays pure and deterministic.

Key formats (must match DatabasePort when it executes the plan):

- Lab: `${patientId}|${testName}|${testDate ?? ''}|${valueNumeric ?? ''}`
- Vax: `${patientId}|${vaccineName}|${dateGiven ?? ''}|${doseNumber ?? 0}`

## Downstream work (not in scope)

- DIS-024 audit-log writer should consume `plan.skipped` and emit one
  audit row per skipped entity (reason + input snapshot).
- The DatabasePort adapter must execute inserts inside a single
  transaction; on any failure, roll back and flip the extraction back
  to `pending_review` (TDD §13 guard 3).

## Files touched

- `dis/src/core/promotion.ts` (new)
- `dis/tests/unit/promotion.test.ts` (new)

## Commits

1. `22a2de2` test(DIS-023): failing promotion tests — CS-10 latest-only, CS-11 dedup
2. `8bebe5a` feat(DIS-023): promotion-plan builder — CS-10 + CS-11 enforced in code
3. `a70af19` test(DIS-023): add partial-dedup test to meet test-count gate

## Verify Report

| #   | Check                                                          | Expected                                  | Actual | Pass |
| --- | -------------------------------------------------------------- | ----------------------------------------- | ------ | ---- | --- |
| 1   | `test -f dis/src/core/promotion.ts`                            | EXISTS                                    | EXISTS | Y    |
| 2   | `grep -c "latest" dis/src/core/promotion.ts`                   | ≥ 1                                       | 7      | Y    |
| 3   | `grep -c "discharge_summary" dis/src/core/promotion.ts`        | ≥ 1                                       | 5      | Y    |
| 4   | `grep -c "skipped" dis/src/core/promotion.ts`                  | ≥ 2                                       | 6      | Y    |
| 5   | `grep -cE "it\(                                                | test\(" dis/tests/unit/promotion.test.ts` | ≥ 8    | 8    | Y   |
| 6   | `grep -c "TSB" dis/tests/unit/promotion.test.ts`               | ≥ 1                                       | 17     | Y    |
| 7   | `grep -rE "from ['\"].*(adapters)/" dis/src/core/promotion.ts` | EMPTY                                     | EMPTY  | Y    |
| 8   | `git log --oneline -2`                                         | 2 lines                                   | OK     | Y    |

Test run: `npx vitest run tests/unit/promotion.test.ts` — **8/8 passing**
(Test Files 1 passed; Tests 8 passed; duration ~0.5 s).

Port-validator (DIP): core imports zero adapters. Confirmed via rg.

## Constraints honored

- Strict TS, no `any`, no `// @ts-ignore`.
- Pure function; deterministic; no I/O.
- `readonly` everywhere on public types; no in-place mutation across
  the module boundary.
- LF line endings; files only under `dis/src/core/`, `dis/tests/unit/`,
  `dis/handoffs/`.
