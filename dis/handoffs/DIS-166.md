# DIS-166 — Chaos: OCR returns garbage (CS-7 batched)

## Summary
Chaos test simulates OCR returning non-medical garbage text. Structuring heuristic fails closed (`success: false`, reason `non-medical-content`), and the state machine transitions `structuring -> failed` via `{ kind: 'fail' }`. A positive control confirms real medical text passes the heuristic. A third test asserts an invalid `structured` event from `uploaded` throws `InvalidStateTransitionError` rather than writing a bad state.

## Clinical safety
CS-7 (fail-closed on garbage OCR) is upheld: bogus OCR cannot reach `promoted`. The pipeline terminates in `failed`, requiring human review / re-ingest rather than silently emitting a clinical artefact.

## Files
- `dis/tests/chaos/ocr-garbage.test.ts` (new)

## Verify
- VERIFY-1: `npx vitest run tests/chaos/ocr-garbage.test.ts` -> 3 tests PASS.
- VERIFY-2: `npx vitest run -t "extraction ends in failed state"` -> 1 PASS, rest skipped.
- VERIFY-3: `npx tsc --noEmit` -> no errors in `tests/chaos/ocr-garbage.test.ts` (pre-existing unrelated errors in `src/http/server.ts` for `@hono/node-server` types, out of scope).

## Notes
State machine event names (`'fail'`, `'structured'`) matched the brief exactly — no adaptation needed. `structureOrFail` stays local to the test; production structuring adapter already fails closed, this test codifies the CS-7 invariant at the pipeline boundary.
