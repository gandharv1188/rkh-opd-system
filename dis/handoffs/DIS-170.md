# DIS-170 Handoff — Clinical-Safety Dry-Run Report

## 1. Ticket
DIS-170: Clinical-safety dry-run report for clinician sign-off.

## 2. Scope
Written report aggregating audit + red-team + chaos evidence against every CS rule in `01_product/clinical_safety.md`.

## 3. Files changed
- `dis/document_ingestion_service/10_handoff/clinical-safety-dry-run.md` (new, ~180 lines)
- `dis/handoffs/DIS-170.md` (new, this file)

## 4. Approach
Read the authoritative source spec `01_product/clinical_safety.md` first. Its CS-1..CS-12 numbering/titles differ from the dispatch brief — used the spec's numbering (spec is source of truth). Each CS section follows the mandated 5-element shape: guarantee / enforcement / test evidence / residual risks / clinician sign-off box.

## 5. CS rules covered
All 12 CS rules from the source spec: CS-1 (no unverified promotion), CS-2 (raw preservation), CS-3 (extraction FK), CS-4 (no silent overwrite), CS-5 (reject permanent), CS-6 (field-level edit log), CS-7 (explicit policy), CS-8 (PII/RLS), CS-9 (normalization audit), CS-10 (discharge latest-only), CS-11 (dedupe), CS-12 (no unverified OCR to Rx generator).

## 6. Evidence references
Each section names concrete test files (`dis-verify/test/*`, `dis-promote/test/*`, `dis-ingest/test/*`, `generate-prescription/test/*`, `schema/test/*`) and the DIS tickets that implemented the control (DIS-031, DIS-034, DIS-092, DIS-120, DIS-121, DIS-122, DIS-130, DIS-140, DIS-145, DIS-152).

## 7. Open questions (flagged for sign-off)
1. DIS-145 migration not yet applied to staging Supabase.
2. DIS-163 / DIS-168 / DIS-171 deferred pending staging project.
3. RLS hardening decision for pilot (POC `anon_full_access` vs CS-8 nurse-role RLS).
4. CS-4 "supersede" UI gap — pilot-blocker or paper workaround?
5. Weekly clinician audit logbook location + cadence confirmation.

## 8. Verification
- `VERIFY-1`: file exists at `dis/document_ingestion_service/10_handoff/clinical-safety-dry-run.md` ✅
- `VERIFY-2`: `grep -c "^## CS-"` = **12** (>= 12 required) ✅
- 12 sign-off checkboxes present (one per CS section).

## 9. Out of scope
- Running the tests themselves (this is a report; tests live in sibling tickets).
- Applying DIS-145 to staging (DevOps action).
- Kill-switch/cost-guardrail/chaos drills (DIS-163/168/171, deferred).

## 10. Follow-ups
- Circulate report to Dr. Lokender Goyal + clinical reviewer for sign-off.
- Once staging exists, re-run relevant sections for CS-3 / CS-12 / "out-of-band" safeguards.
- Open a DIS ticket to build the CS-4 supersede UI if pilot requires it.

## 11. Status
Doc-only. No code, no migrations, no edge-function changes. Safe to merge after review.
