# DIS-045 — Cost calculator integration (per-run aggregate)

**Branch:** `feat/dev-b2-errors-schema-cost`
**Worktree:** `.claude/worktrees/dev-b2-errors-schema-cost`
**Status:** Implemented, verified green.

## 1. What shipped

- `dis/tests/integration/cost-aggregate.test.ts` — 2 tests:
  1. One full orchestrator run through the `ocr_scan` routing branch.
     FakeOcrAdapter returns `pageCount=3` + `tokensUsed`;
     FakeStructuringAdapter returns `tokensUsed`. After the run, the test
     aggregates OCR + LLM line items via `calculateCost()` and asserts:
     - both OCR cost and LLM (input+output) costs are > 0,
     - `total_micro_inr == input + output + ocr` (sum invariant),
     - matches a hand-computed expected total against the fixed rate
       table (Datalab page rate + Haiku per-token rates).
  2. Additivity: two standalone `calculateCost()` calls sum correctly.

## 2. Wiring / adapter choices

- `FakeFileRouterAdapter` is keyed by `filename` — the orchestrator's
  `process()` calls `fileRouter.route({ filename: '' })` internally, so
  the script maps the empty string to `{ kind: 'ocr_scan', pageCount: 3 }`.
- `FakeOcrAdapter.keyOf()` uses the first page buffer as a string key.
  The orchestrator passes `Buffer.alloc(0)` (length 1 fallback) so the
  synthetic key lands on `pages:1/image/jpeg`.
- Fixed `RateTable` is injected into every `calculateCost()` call so the
  test is independent of env vars.

## 3. Cost aggregation is done in the test, not the orchestrator

The orchestrator layer does not currently emit an aggregated
`CostBreakdown` — that is owned by the forthcoming DIS-149 cost ledger
writer. Per the Wave-2b brief ("aggregate yourself in the test from
fake call records"), the test recombines the scripted OCR + structuring
results through `calculateCost()` to prove the sum matches.

This isolates DIS-032 correctness (per-provider line items) from DIS-149
persistence while still exercising a single real orchestrator run end
to end.

## 4. VERIFY

- `test -f dis/tests/integration/cost-aggregate.test.ts` → `EXISTS`
- `cd dis && npx vitest run tests/integration/cost-aggregate.test.ts` →
  `Test Files 1 passed`, `Tests 2 passed`, exit 0.
- `grep -cE "CostBreakdown|micro" dis/tests/integration/cost-aggregate.test.ts` → **21** (≥ 1).

## 5. Follow-ups

- DIS-149 (cost ledger) should hook into orchestrator.runPipeline and
  persist the aggregated CostBreakdown per extraction_id; replace this
  test's manual aggregation with reading a real ledger row.
- Consider also testing the `datalab+haiku` hybrid provider path once an
  adapter emits that tag.
