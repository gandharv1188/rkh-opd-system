# DIS-125 — StatusBadge component

**Owner:** dev-e-status-badge
**Branch:** feat/dev-e-status-badge
**Wave:** 6b-1

## Delivered
- `dis/ui/src/components/StatusBadge.tsx` — React component + exported `COLOUR_MAP` + `ExtractionState` type. 8 states: uploaded / preprocessing / ocr / structuring / ready_for_review / promoted / rejected / failed. Inline styles (bg, fg, padding, radius). `data-testid="status-badge-{state}"`.
- `dis/ui/tests/status-badge.spec.ts` — Playwright test (vitest not installed) asserting COLOUR_MAP shape + semantic colour-group uniqueness. Exact `it()` name `"renders each state with unique colour"`.

## Test-runner choice
- **Vitest is NOT in `dis/ui/package.json`** — only `@playwright/test` is available. Substituted Playwright for VERIFY-1/2 per brief's fallback guidance.
- Test imports COLOUR_MAP directly (no browser rendering needed), so it runs as a pure-logic Playwright test. The webServer boots (unavoidable with current playwright.config.ts) but the test itself does no page interaction.

## VERIFY
- VERIFY-1: `npx playwright test tests/status-badge.spec.ts` → 1 passed (5.9s)
- VERIFY-2: `npx playwright test -g "renders each state with unique colour"` → 1 passed (4.0s)
- VERIFY-3: `tsc --noEmit` → exit 0

## State-machine note
`dis/src/core/state-machine.ts` defines 10 states including `auto_approved` and `verified`, which are not in the brief's `ExtractionState`. These are internal workflow states not shown on UI surface; the 8 UI-visible states in the brief match what the Queue/Verify pages should display. Follow-up may be needed if `auto_approved` / `verified` are also exposed to the UI later — document here: **DIS-125-followup-a**.

## Follow-ups
- **DIS-125-followup-vitest**: install `vitest` + `@testing-library/react` in `dis/ui/` so future component tests can mount and assert on rendered DOM (currently Playwright requires a webServer and is heavier for pure-logic tests).
- **DIS-125-followup-a**: confirm whether `auto_approved` / `verified` need badge representation or stay internal-only.

## File paths (absolute)
- `E:\projects\AI-Enabled HMIS\radhakishan-prescription-system-folder\radhakishan-prescription-system\.claude\worktrees\dev-e-status-badge\dis\ui\src\components\StatusBadge.tsx`
- `E:\projects\AI-Enabled HMIS\radhakishan-prescription-system-folder\radhakishan-prescription-system\.claude\worktrees\dev-e-status-badge\dis\ui\tests\status-badge.spec.ts`
