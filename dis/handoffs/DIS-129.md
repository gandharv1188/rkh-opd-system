# DIS-129 — ConflictDialog

## Scope
Dialog component rendered when `approve` (or similar mutation) returns HTTP 409 VersionConflict. Shows the current server version/status and offers a "Reload & re-review" action so the operator re-reads before retrying.

## Files
- `dis/ui/src/components/ConflictDialog.tsx` (new) — controlled component (`open`, `currentVersion`, `currentStatus`, `onReload`, `onCancel`). `role="dialog"`, `aria-modal="true"`, labelled by `#conflict-title`. Returns `null` when `!open`.
- `dis/ui/tests/conflict-dialog.spec.ts` (new) — Playwright smoke: `'shell renders'` + `'shows dialog on 409'` (stubbed at shell level; real harness wiring not yet available per brief).

## Contract
```ts
interface ConflictDialogProps {
  open: boolean;
  currentVersion: number;
  currentStatus: string;
  onReload: () => void;
  onCancel: () => void;
}
```
`data-testid`s: `conflict-dialog`, `conflict-reload`, `conflict-cancel`.

## VERIFY
- VERIFY-1/2/3 (`npx playwright test`, `npm run typecheck`): NOT RUN — source `dis/ui/node_modules` is an empty directory in this workspace, so neither `@playwright/test` nor `typescript` resolve. Brief explicitly prohibits `npm install`. Junction from worktree `node_modules` → source was created as instructed but target is empty. Same condition affects sibling worktrees (e.g. `dev-e-theme`). Environment fix is out of scope for this ticket.
- Component text/markup matches brief verbatim.

## Follow-ups
- Wire `ConflictDialog` into the approve/reject flow (DIS-adjacent ticket) — consumer should capture 409 response body, map `currentVersion` + `currentStatus`, and on reload re-fetch the extraction envelope before re-opening review.
- Replace the shell-smoke test with a real 409 harness once the extract-approve client hook lands.
