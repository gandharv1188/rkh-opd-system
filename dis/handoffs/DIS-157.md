# DIS-157 Handoff — Duplicate Upload Storm Runbook (CS-4)

**Status:** Complete (CS-4 batched sign-off).
**Branch:** `feat/dev-f-rb-dupstorm`
**Owner:** `dev-f-rb-dupstorm`

## Deliverable

- `dis/document_ingestion_service/09_runbooks/duplicate-storm.md` — new runbook, 6 `## Step` sections (Detect, Triage, Contain, Notify, Audit, Post-mortem).

## Summary

Runbook covers the CS-4 duplicate-upload class end to end:

- **Detect** via `dis_ingestion_duplicate_rate` and queue depth.
- **Triage** distinguishes legitimate concurrent reception-desk re-submits from client retry loops vs. abuse — different containment paths.
- **Contain** with DIS-102 per-operator rate limiter (ephemeral `max_tokens` lowering); explicitly warns against disabling DIS-037 dedupe.
- **Notify** operators via DIS-122 `DuplicateBanner`; clinician follows CS-4 override protocol if the duplicate is actually a distinct result.
- **Audit** via the DIS-037 dedupe check to prove no `payload_hash` had >1 promotion.
- **Post-mortem** classifies CS-4-LEG / CS-4-BUG / CS-4-SEC; mandates client patch before closing if BUG.

Cross-references DIS-037, DIS-102, DIS-122 as required.

## Verify

```
VERIFY-1: ls dis/document_ingestion_service/09_runbooks/duplicate-storm.md  # EXISTS
VERIFY-2: grep -c "^## Step" dis/document_ingestion_service/09_runbooks/duplicate-storm.md  # 6 (>=5)
```

## Follow-ups

None for DIS-157. Downstream dependencies: when DIS-037 audit query canonical form lands, add a one-liner link to it from Step 5.
