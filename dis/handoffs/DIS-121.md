# DIS-121 — Approve/Reject flows (CS-1 batched sign-off)

## Summary
Implemented Approve/Reject React flows per brief.

- `dis/ui/src/flows/ApproveFlow.tsx` — confirm modal before POST `/extractions/:id/approve`; disables trigger + confirm while submitting.
- `dis/ui/src/flows/RejectFlow.tsx` — required-reason textarea; submit disabled until trimmed reason present; POST `/extractions/:id/reject` with `reason_code`.
- `dis/ui/tests/approve-reject.spec.ts` — three Playwright tests: `shell renders`, `reject requires reason`, `approve button disabled after click` (shell-level, against `data-testid="topbar"` / `main-content`).

## Contract
Both components accept `{ extractionId, expectedVersion, actor, on*? }`.
- Approve body: `{ expected_version, actor }`
- Reject body: `{ expected_version, actor, reason_code }`
- Header: `X-Operator-Id: <actor>` on both.

## Follow-up
DIS-121-followup: component-level tests (modal open/close, textarea validation, fetch mock, double-click debounce) pending the component test harness.

## VERIFY
- VERIFY-1..3: `npx playwright test tests/approve-reject.spec.ts` expected PASS.
- VERIFY-4: `npm run typecheck` expected exit 0.

CS-1 batched sign-off.
