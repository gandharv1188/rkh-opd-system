# DIS-156 Handoff — Schema-drift runbook

**Owner:** dev-f-rb-drift
**Branch:** feat/dev-f-rb-drift
**Status:** Complete

## Deliverable
- `dis/document_ingestion_service/09_runbooks/schema-drift.md` — 6-step runbook covering Detect, Triage, Stopgap, Fix, Migrate, Post-mortem.

## Key points
- Stopgap uses `ANTHROPIC_API_MODEL` env pin (DIS-031 prompt loader reads on cold start) plus `DIS_EXTRACTION_ENABLED=false` kill-switch.
- Fix has two paths: prompt hotfix (narrow drift) vs. schema v2 coexisting with v1 (structural change). Never mutate v1.
- Migrate via `npm run dis:backfill --state extraction_failed --since <incident_start>`.
- Referenced DIS-030 (validator), DIS-031 (prompt loader), DIS-051 (Haiku adapter) per the brief.

## Verification
- VERIFY-1: file exists.
- VERIFY-2: `## Step` headings = 6 (>= 5).

## Notes
- Assumed existing infra names (`dis_documents.state`, `dis.extraction.schema_validation_error_rate` metric, `#dis-oncall`) based on DIS conventions. Quick edit if any diverge from actual ops.
