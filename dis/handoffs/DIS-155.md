# DIS-155 — Cost-Spike Runbook — Handoff

**Owner:** dev-f-rb-cost
**Branch:** feat/dev-f-rb-cost
**Status:** Complete

## Deliverable

- `dis/document_ingestion_service/09_runbooks/cost-spike.md` — 5-step runbook
  covering Detect → Triage → Cap → Root-cause → Post-mortem.

## Key references

- DIS-149 — cost ledger (`dis_cost_ledger`, `cost_micro_inr_total` metric)
- DIS-165 — daily-budget guardrail (`DIS_DAILY_BUDGET_INR`,
  `BUDGET_EXCEEDED` response)

## Triage query (copy from runbook)

```sql
SELECT provider, operation,
       COUNT(*) AS calls,
       SUM(cost_micro_inr)/1e6 AS inr_spent,
       AVG(cost_micro_inr)/1e6 AS inr_per_call
FROM   dis_cost_ledger
WHERE  created_at >= date_trunc('day', now())
GROUP  BY provider, operation
ORDER  BY inr_spent DESC;
```

## Mitigation hierarchy

1. Lower `DIS_DAILY_BUDGET_INR` (guardrail sheds load).
2. Adapter kill-switch flag (`DIS_ADAPTER_<NAME>_ENABLED=false`).
3. Drop retry cap (`DIS_ADAPTER_MAX_RETRIES=1`).

## Root-cause checklist

Looped retries · runaway input size · silent model switch · cache-hit-rate
regression · abusive tenant.

## Verification

- `ls dis/document_ingestion_service/09_runbooks/cost-spike.md` — exists.
- `grep -c "^## Step" …/cost-spike.md` — 5.

## Follow-ups

None. Post-mortem template referenced at
`dis/document_ingestion_service/10_incidents/` — create folder/template if
not present as part of first real incident.
