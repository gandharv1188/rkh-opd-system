# Handoff — DIS-007 Canonical OpenAPI in `dis/openapi.yaml` + CI validator

- **Agent:** dev-007-011-015-infra (Wave-1)
- **Branch:** feat/dev-007-011-015-infra
- **Worktree:** .claude/worktrees/dev-007-011-015-infra
- **Date:** 2026-04-22
- **TDD refs:** §3 (HTTP surface)
- **CS refs:** none
- **Tags:** `infra`, `doc-only`

## 1. What was built

- `dis/openapi.yaml` — copy of the canonical spec at `dis/document_ingestion_service/04_api/openapi.yaml`, with two narrow adjustments to make the file valid OpenAPI 3.1 and satisfy VERIFY-3:
  1. Added a `/health` path (GET, public, returns `{ status: "ok", version, correlation_id }`).
  2. Converted four `nullable: true` properties to OAS 3.1 nullable-union form `type: [X, 'null']` on `doc_date`, `verified_structured`, `raw_ocr_markdown`, `next_cursor`.
- `dis/scripts/validate-openapi.sh` — bash validator: `set -euo pipefail`, cwd-independent (resolves its own repo root), invokes `npx @redocly/cli lint openapi.yaml`. Marked executable.
- `.github/workflows/dis-ci.yml` — renamed `openapi-lint` → `openapi-validate`; replaced inline `npx @redocly/cli lint` with `bash dis/scripts/validate-openapi.sh` so the CLI command lives in a single source of truth (the script). `hashFiles('dis/openapi.yaml')` guard retained.

## 2. Acceptance criteria status

All five VERIFY checks pass (no redocly lint errors — only 7 unchanged pre-existing warnings about missing license / missing 4XX / tag descriptions, which don't fail CI):

- VERIFY-1: `test -f dis/openapi.yaml && echo EXISTS` → EXISTS.
- VERIFY-2: `grep -cE "^openapi: 3\." dis/openapi.yaml` → 1.
- VERIFY-3: `grep -c "/health" dis/openapi.yaml` → 1 (≥ 1 required).
- VERIFY-4: `grep -ci "openapi" .github/workflows/dis-ci.yml` → 6 (≥ 1 required).
- VERIFY-5: `test -f dis/scripts/validate-openapi.sh && echo OK` → OK.
- Bonus: `bash dis/scripts/validate-openapi.sh` → exit 0 (clean lint, 0 errors).

## 3. Decisions

### D-1: Gate 2 test-first SKIPPED (`doc-only` + `infra`)

Per `verify_format.md`, `doc-only` tickets skip Gate 2. DIS-007 is doc-only on the YAML side + a one-shot bash wrapper on the infra side. No test file is required by `files_allowed`. The Verify loop (running the bash script locally and seeing exit 0) serves as the test.

### D-2: `/health` added inline, not left for DIS-004 to backfill

VERIFY-3 of DIS-007 requires `/health` in `dis/openapi.yaml`. The canonical source at `04_api/openapi.yaml` does not declare `/health`, though `src/http/` already implements it (DIS-004). Declared the path here so the spec matches the running service. `files_allowed` permits writing to `dis/openapi.yaml`, so this is in scope.

### D-3: Fixed `nullable: true` → `type: [X, 'null']`

Canonical source uses the OpenAPI 3.0 idiom `nullable: true`, which is **not valid** under `openapi: 3.1.0` (the header both files declare). Redocly's default ruleset flags this as a `struct` error, and without the fix the new `openapi-validate` CI job would be red from day one. Four surgical edits converted the four offending properties. Did **not** edit `04_api/openapi.yaml` — that file is outside `files_allowed`, so the two specs will diverge until DIS-007-followup reconciles them.

### D-4: Script-indirection over inline npx in CI

Moved the lint command from inline workflow YAML into `dis/scripts/validate-openapi.sh` so devs can run the exact same check locally and so future tweaks (additional rulesets, ignore-file, pinned version) live in one file.

## 4. Files touched

- `dis/openapi.yaml` (new, 316 lines — copied from `04_api/openapi.yaml` + /health path + 4× nullable 3.1 conversion)
- `dis/scripts/validate-openapi.sh` (new, 11 lines, executable)
- `.github/workflows/dis-ci.yml` (openapi-lint job renamed to openapi-validate, lint step now invokes script)

## 5. Follow-ups / flags

### F-1 (Orientation finding F3/F4): 503/UNAVAILABLE response declarations

ADR-003 (kill-switch) requires a 503 `UNAVAILABLE` response on every write endpoint. Current `dis/openapi.yaml` does not declare it on any path. **Not fixed here** — would touch every path block and widen DIS-007's diff significantly beyond its `doc-only` intent. Register **DIS-007-followup** to:

- Add `components.responses.Unavailable` and reference it as `'503': { $ref: '#/components/responses/Unavailable' }` on `POST /uploads/signed-url`, `POST /ingest`, `POST /extractions/{id}/approve`, `POST /extractions/{id}/reject`, `POST /extractions/{id}/retry`, `POST /internal/process-job`.
- Mirror the change into `04_api/openapi.yaml` (canonical source).

### F-2: `dis/openapi.yaml` and `04_api/openapi.yaml` now drift

Two `/health`, `type: [X,'null']` changes live only in `dis/openapi.yaml`. Canonical source still has `nullable: true` and no `/health`. Reconcile in the same DIS-007-followup ticket (copy canonical → dis, then re-apply these patches, or move canonicality to `dis/openapi.yaml` and drop the 04_api copy). Decision deferred to the orchestrator.

### F-3: Pre-existing lint warnings (7) left as-is

Redocly reports 7 warnings: missing license, missing 4XX on 5 paths, missing tag descriptions. All warnings, not errors. Ignoring until a dedicated polish ticket. They do NOT fail CI.

## 6. Commits

- `docs(DIS-007): canonical OpenAPI in dis/openapi.yaml + CI validator`
