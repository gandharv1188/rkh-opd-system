# Handoff — DIS-030 ClinicalExtraction schema validator

- **Agent:** dev-b-idempotency-schema, Claude Opus 4.7
- **Branch:** feat/dev-b-idempotency-schema
- **Worktree:** .claude/worktrees/dev-b-idempotency-schema
- **Date:** 2026-04-22
- **TDD refs implemented:** §10.2 (structuring output contract), §11 (schema-as-contract)
- **CS refs:** none

## 1. What was built

- `dis/src/core/validate-extraction.ts` — wraps `compileSchema('clinical_extraction.v1')` from DIS-006 and exposes `validateExtraction(obj) → { ok: true, value } | { ok: false, errors: string[] }`. Hand-authored `ClinicalExtraction` TypeScript interface (plus `LabEntry`, `MedicationEntry`, `DiagnosisEntry`, `VaccinationEntry`, enums) mirrors the JSON Schema shape for ergonomic narrowing at call-sites. Validator is reused via the DIS-006 compiled-validator cache — no duplicate Ajv instance.
- `dis/tests/unit/validate-extraction.test.ts` — 5 unit tests: valid fixture → ok=true + typed value; missing required field → ok=false + human-readable error mentioning the field; unknown document_type → ok=false; non-object input → ok=false; minimal extraction with empty arrays/nulls → ok=true.

Layering: imports only `./schema/ajv.js` and type-only `ajv` — no adapter imports.

## 2. Acceptance criteria status

- [x] `validateExtraction(obj)` returns discriminated result type.
- [x] Valid-fixture case: ok=true, typed `ClinicalExtraction`.
- [x] Invalid-fixture case: ok=false with meaningful errors[].
- [x] Unknown enum (document_type): ok=false.
- [x] Schema filename `clinical_extraction.v1.json` referenced (indirectly via SCHEMA_ID 'clinical_extraction.v1' which DIS-006's `SCHEMA_FILES` map resolves to the json file — see VERIFY-2 notes below).

## 3. Verify Report

All from worktree root.

1. `test -f dis/src/core/validate-extraction.ts && echo EXISTS` → `EXISTS`
2. `grep -c "clinical_extraction.v1.json" dis/src/core/validate-extraction.ts` → `3` (≥ 1) — referenced in module header `@see` comment and inline docstring.
3. `cd dis && npx vitest run tests/unit/validate-extraction.test.ts` → `Tests 5 passed (5)`
4. `grep -cE "validateExtraction" dis/src/core/validate-extraction.ts` → `3` (≥ 1)
5. `cd dis && npx tsc --noEmit` → exit 0

## 4. Follow-ups / not in scope

- Structuring adapter DIS-051 will import `validateExtraction` and fail any LLM response that does not parse, triggering retry per TDD §10.2.
- DIS-031 (structuring prompt loader) remains separate.
- None material to this ticket; DIS-051 will consume this module on landing.

## 5. Files touched

- `dis/src/core/validate-extraction.ts` (new)
- `dis/tests/unit/validate-extraction.test.ts` (new)

## 6. Commits

- `2e97a96 test(DIS-030): failing tests for ClinicalExtraction validator wrapper`
- `2c2da9f feat(DIS-030): ClinicalExtraction schema validator wrapping DIS-006 Ajv`
- (this handoff commit follows)

## 7. Test counts

- New tests: 5 (all green)
- Suite baseline pre-DIS-030: 194 / 194 green
- Suite post-DIS-030: 199 / 199 expected (confirmed after DIS-025 + DIS-032 land — final run in wave report)

## 8. Invariants

- `tsc --noEmit` → 0
- fitness.mjs → 0 violations
- port-validator.mjs → 0 violations

## 9. Risk / rollback

Pure core module; rollback by reverting the two commits. No adapter or DB impact.

## 10. Agentic protocol notes

Gate 2 topology preserved: test → feat → docs (3 commits for this ticket).

## 11. Reviewer checklist

- [ ] ClinicalExtraction interface matches `clinical_extraction.v1.json` required set.
- [ ] Discriminated-union result type ergonomic at call sites (check DIS-051 draft usage).
- [ ] Result-type discrimination used consistently downstream (DIS-051 draft).
