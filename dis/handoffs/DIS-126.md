# DIS-126 Handoff — UI E2E Scenarios

## Scope delivered

Three Playwright E2E specs under `dis/ui/tests/e2e/`:

- `happy-path.spec.ts` — loads `/`, asserts `topbar`, `sidebar`, `main-content` test-ids are visible; stubs `**/extractions*` with empty list.
- `reject.spec.ts` — app-shell reachability smoke; asserts `main-content` is visible.
- `duplicate-override.spec.ts` — app-shell reachability; asserts `topbar` is visible (stands in for kill-switch-off path).

Code matches the brief verbatim.

## Scope reality (important)

The current `App.tsx` does not yet have routing to Queue / Verify / Duplicate-override UI. Full user journeys (approve, reject, duplicate override flow) cannot be driven end-to-end. These specs are intentionally **app-shell smoke scenarios** that will pass once rendering is wired with the three documented test-ids.

Follow-up: **DIS-126-followup** — expand these specs to exercise full journeys after App.tsx gets Router + Queue/Verify pages. That follow-up should:
1. Seed data (or intercept Supabase) for a pending extraction.
2. Drive the reviewer UI to approve / reject / force duplicate override.
3. Assert backend state transitions (status, audit entries).

## Verification status

Brief specified:
```
VERIFY-1: cd dis/ui && npx playwright test tests/e2e/happy-path.spec.ts  # PASS
VERIFY-2: cd dis/ui && npx playwright test tests/e2e/reject.spec.ts      # PASS
VERIFY-3: cd dis/ui && npx playwright test tests/e2e/duplicate-override.spec.ts  # PASS
```

**Not executed locally.** The worktree's `node_modules` junction points at the source `dis/ui/node_modules`, which is empty in this session (no playwright binary, no `@playwright/test` package), and the brief explicitly forbids `npm install`. `npx playwright test` fails with `ERR_MODULE_NOT_FOUND` for `@playwright/test` at config load.

Assumption: verification will happen in CI or after a sibling wave runs `npm install`. Specs are syntactically complete, import-only from `@playwright/test`, and depend only on test-ids (`topbar`, `sidebar`, `main-content`) that downstream App.tsx work must expose. Brief inspection confirms:
- Imports match sibling specs in `dis/ui/tests/` (identical `@playwright/test` import).
- Selectors (`getByTestId`) are standard Playwright API.
- `page.route` stubbing pattern mirrors the brief exactly.

## Files added

- `dis/ui/tests/e2e/happy-path.spec.ts`
- `dis/ui/tests/e2e/reject.spec.ts`
- `dis/ui/tests/e2e/duplicate-override.spec.ts`
- `dis/handoffs/DIS-126.md`

## Next owner

DIS-126-followup: wire App.tsx routing + flesh out the three specs into real journeys.
